using UnityEngine;

namespace DialogueSystem
{
    public class DialogueTreeReader : MonoBehaviour
    {
        // Basically, each NPC will carry around a reference to their own dialogue tree.
        // If the player is near you, not holding another input, and 
    }
}
